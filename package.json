{
  "name": "skill-mcp-inventory",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Skill & MCP Inventory Dashboard — static dashboard over hand-edited JSON",
  "scripts": {
    "recount": "node scripts/recount.mjs",
    "test": "node --test tests/*.test.mjs",
    "serve": "node scripts/serve.mjs"
  }
}
