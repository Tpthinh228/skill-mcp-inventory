{
  "name": "skill-mcp-inventory",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Skill & MCP Inventory Dashboard — local discovery + static dashboard",
  "scripts": {
    "inventory:scan": "node scripts/discover-skills.mjs && node scripts/discover-mcp.mjs",
    "inventory:verify": "node scripts/verify.mjs",
    "inventory:github": "node scripts/github-metadata.mjs",
    "inventory:build": "node scripts/build-inventory.mjs",
    "inventory:all": "npm run inventory:scan && npm run inventory:github && npm run inventory:verify && npm run inventory:build",
    "test": "node --test tests/*.test.mjs",
    "serve": "node scripts/serve.mjs",
    "start": "npm run inventory:all && node scripts/serve.mjs"
  }
}
